pipeline {
    agent any
  
    stages {
        stage('Build') {
            steps {
                // Your build steps (e.g., compile, test)
                sh 'echo "Building the web application"'
            }
        }
        stage('Test') {
            steps {
                // Your test steps
                sh 'echo "Running tests"'
            }
        }
    }
}
